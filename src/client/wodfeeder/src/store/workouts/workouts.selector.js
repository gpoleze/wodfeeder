export const getWorkouts = state => state.workouts.weeksWorkouts;

//TODO - make the next two functions return their respective right values. The current one is just while the code is not ready
export const getViewWorkoutsForm = state => state.workouts.weekFormValues;

export const getEditWorkoutsForm = state => state.workouts.weekFormValues;

