# The full list of properties can be found at https://docs.spring.io/spring-boot/docs/2.0.4.RELEASE/reference/html/common-application-properties.html

## Spring DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DATABaSE_USERNAME}
spring.datasource.password=${DATABASE_PASSWORD}

## Hibernate
spring.jpa.properties.hibernate.default_schema=public

##MIGRATION
spring.flyway.baseline-on-migrate=true

# JWT
wodfeeder.jwt.expiration=36000000
wodfeeder.jwt.secret=${JWT_SECRET}