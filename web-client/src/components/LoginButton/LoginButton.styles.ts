import makeStyles from "@material-ui/core/styles/makeStyles";

const useSytles = makeStyles((theme) => ({
    link: {
        textDecoration: "none",
        color: "inherit",
    },
    button: {
        color: "#fff",
        borderColor: "#fff",
    },
}));

export default useSytles;
